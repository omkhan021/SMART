{
  "name": "social-media-sentiment-analyzer",
  "version": "1.0.0",
  "description": "A full-stack web application that analyzes sentiment and demographics from social media post comments",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["sentiment-analysis", "social-media", "demographics", "web-scraping"],
  "author": "FAANG Level Developer",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
